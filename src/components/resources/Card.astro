---
const {title, image, href } = Astro.props;
---

<a href={href} class="grid grid-cols-3 justify-items-end items-center w-full md:w-auto bg-yellow-50/50 dark:bg-neutral-700 shadow-sm font-semibold py-2 px-8 md:p-4 md:gap-2 rounded-lg hover:-translate-y-1 transition">
    <h3 class="text-lg text-nowrap col-span-2 justify-self-start md:pl-5">{title}</h3>
    <span><img class="max-w-12 md:max-w-28" src={image} alt={title}></span>
</a>